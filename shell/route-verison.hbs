.route(
	{
	version: `${config.routeVersions.{{ camelCase name }} }`,
		url: '/{{ kebabCase name }}',
		schema: schema[`${config.routeVersions.{{ camelCase name }} }`].{{ constantCase verb }},
		method: '{{ constantCase verb }}',
		handler: async request => {
			/* Database Model(s) example
			return Users.query()
			.where('id', request.session.user_id)
			.eager('[clusters]')
			*/
			return {{ constantCase verb }}({request})
		}
	}
)

